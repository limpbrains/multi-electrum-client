# Fulcrum default config (filled in during M3 once we boot the stack).
datadir = /data
bitcoind = bitcoind:18443
rpcuser = ci
rpcpassword = ci
tcp = 0.0.0.0:50001
ssl = 0.0.0.0:50004
peering = false
announce = false
